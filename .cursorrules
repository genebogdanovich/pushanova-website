# Pushanova Website

Edit this file. Keep it short. Prefer facts over style lectures.

## Project

Pushanova is a native push-up training app for iPhone, iPad, and Apple Watch. 
The core value proposition is removing friction: no tripods, no accounts, and no leaderboards. 

- Goal: Build a fast, lightweight, static marketing website optimized for 50 global languages.
- Source of product truth: `materials/specs/product/Pushanova-Product-Paper.md` (or equivalent product documents).
- Do not invent features. Match existing copy.

## Stack

- Static HTML generated by a simple Node.js build script.
- Vanilla CSS. Use simple, native, modern HTML/CSS paradigms. No hacks, no brittle workarounds, and no magic numbers.
- Localization driven by individual JSON files per language (e.g., `locales/en.json`, `locales/da.json`).
- Hosting: GitHub Pages. The final build output must be 100% static and compatible with standard GitHub Pages deployment.
- NO React, NO Next.js, NO Tailwind, NO complex frameworks.

## Localization & Translation

- **English as the Base & Default:** English (`en.json`) is the absolute source language, base template, and default fallback for the entire project. All translations must originate directly from the English source file. If a localized string or asset is missing, the system must fall back to the English equivalent.
- **One Language Per File:** Architecture must be scalable and token-efficient. Do not merge languages into a single giant JSON.
- **Add a language:** Read only `locales/en.json`. Create `locales/{bcp47}.json` with the same tree. Translate `message` values only; omit `description`. Set `meta.code` and `meta.name` (the name in that language). Do not translate placeholders, HTML tags, **Pushanova**, Apple product names, or in-app UI labels inside `<strong>`. Do not open other `locales/*.json` files. Do not edit templates. Run `npm run build`.
- **Context is Required:** The English source file (`en.json`) must include descriptive comments for each key. Translators and AI need to know where the string appears, its character limits, and its context. Other locale files may ship `{ "message": "…" }` leaves only.
- **Protect Placeholders:** Never translate variables, placeholders, or HTML tags (e.g., `{rating}`, `{email}`, `<sup>`). They must remain exactly as written in the source.
- **Whole Sentences Only:** Never concatenate text fragments in the code (e.g., never do `"Translated from " + language`). Always translate full strings so grammar structures can shift naturally per language.
- **Write for Translation:** Use simple, direct, and universally understood English. Avoid slang, idioms, abstract terms, or complex sentence structure. 

## Conventions

- **Modern AEO & SEO:** Prioritize Answer Engine Optimization (AEO) and traditional SEO. Use strict, perfect semantic HTML document outlines (H1, H2, H3), clean meta tags, and descriptive `alt` attributes. AI bots and search crawlers must be able to parse the core value proposition instantly.
- **GitHub Pages Routing:** To support clean URLs without a web server (e.g., `/da/` instead of `/da.html`), the build script must generate subfolders with `index.html` files (e.g., `/da/index.html`). Asset paths (CSS, images) must resolve correctly in a static environment.
- **Copy is the product.** Focus on the emotional relief of the features (e.g., "Leave your phone on the desk" rather than "Uses Core Motion APIs").
- **Privacy policy is English-only.** Do not localize or translate `materials/copy/privacy.md`. The privacy policy remains in original English.
- **Start Where You Are.** The tone should be welcoming. Do not write copy that sounds like an aggressive bootcamp drill sergeant. 
- **Native Dark Mode.** Support dark mode strictly through the CSS `@media (prefers-color-scheme: dark)` query. Do not build JavaScript theme toggles or buttons. Rely entirely on the user's OS preference.

## Styling

- **Mobile-first, full-size on desktop.** Base styles for a phone. At larger viewports, type, spacing, and the reading column grow. Do not keep a phone-width layout on desktop.
- **System sans, Apple first.** `font-family: system-ui, -apple-system, sans-serif;`. No webfonts for marketing copy. Let the OS pick the face for each `lang` (Latin, Indic, CJK, Arabic, and the rest).
- **Tokens** as CSS custom properties. Follow `prefers-color-scheme`.

  Light: page `#FFFFFF`, text `#1D1D1F`, accent `#045DE9` (app blue).
  Dark: page `#000000`, text `#F5F5F7`, accent `#4C8DF6` (same hue, lightened for contrast).

  Use accent for links and emphasis only. Solid fills with white text keep `#045DE9` in both schemes — the dark-mode accent is too light for that. Do not invert `#045DE9` for dark mode; it fails contrast on black.
- **Logical properties** for spacing and alignment. RTL is a first-class layout.
- **Line-height** stays open enough for Indic and other complex scripts (~1.4–1.6). Fluid type with `clamp()` so desktop is not 14px body text. Main column max-width in `ch` or `rem`, not a fixed pixel page.
- **Touch-first:** no hover-only actions; visible focus; wrap, don’t truncate, translated UI. `overflow-wrap` for long words and URLs.
- **Do not** use letter-spacing on copy, `text-transform: uppercase` on translated UI, or `text-align: justify`.

## Do not

- Do not add accounts, ads, leaderboards, streaks-as-punishment, or social features to the site story.
- Do not invent new features or hardware requirements. 
- Do not generate complex CSS grids, "Bento Boxes," or heavy UI mockups unless explicitly instructed. Stick to plain, semantic HTML tags.
- Do not hardcode English marketing text into the final HTML files (all text routes through the isolated language JSON files).